<script setup>

  // const home = await useItems('home')
  // const settings = await useItems('settings')

  const imageUrl = useImage()
</script>

<template>
  <div>

    <Html class="bg-gray-100">
      <Head>
        <!-- <Title>
          {{ settings.name }}
        </Title>
        <Meta name="description" :content="settings.description" />
        <Link rel="icon" type="image/x-icon" :href="imageUrl(settings.icon)" /> -->
      </Head>
    </Html>

    <!-- main content -->

  </div>
</template>